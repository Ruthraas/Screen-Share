# URL do backend em produção (issue #82) — usada automaticamente pelo Vite
# em qualquer build de produção (`npm run build`, `tauri build`), tem
# prioridade sobre `.env.local` só nesse modo (não afeta `npm run dev`).
# Não é segredo, só um endereço público — pode ficar no Git.
VITE_API_URL=https://screenshare-backend-tj9j.onrender.com
