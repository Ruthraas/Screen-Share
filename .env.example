# URL base do backend próprio (issue #30/#60). Padrão local: backend/.env.example PORT=8787.
# Build de produção usa `.env.production` (commitado, não segredo) em vez
# deste — ver issue #82.
VITE_API_URL=http://127.0.0.1:8787
